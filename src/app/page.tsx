import ImageEditorClient from './image-editor-client';

export default function HomePage() {
  return (
    <main className="min-h-screen bg-background">
      <ImageEditorClient />
    </main>
  );
}
